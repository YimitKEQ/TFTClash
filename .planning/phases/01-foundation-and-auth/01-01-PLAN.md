---
phase: 01-foundation-and-auth
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - next.config.js
  - tailwind.config.js
  - postcss.config.js
  - tsconfig.json
  - src/app/layout.tsx
  - src/app/globals.css
  - src/app/page.tsx
  - src/lib/constants.ts
  - src/components/ui/Panel.tsx
  - src/components/ui/Btn.tsx
  - src/components/ui/HexBg.tsx
  - src/components/layout/Navbar.tsx
autonomous: true
requirements:
  - FOUND-01

must_haves:
  truths:
    - "Next.js dev server starts without errors"
    - "Tailwind classes compile correctly with the project color tokens"
    - "Design tokens match the prototype (bg #08080F, panels #111827, purple #9B72CF, gold #E8A838, teal #4ECDC4)"
    - "Playfair Display and Barlow Condensed fonts load from Google Fonts"
    - "Shared layout wraps every page with Navbar and HexBg background"
  artifacts:
    - path: "package.json"
      provides: "Next.js 14 + Tailwind + Supabase client dependencies"
    - path: "tailwind.config.js"
      provides: "Extended color palette with all prototype design tokens"
    - path: "src/app/layout.tsx"
      provides: "Root layout with font imports, metadata, and shared shell"
    - path: "src/lib/constants.ts"
      provides: "PTS map, TIERS, RANKS, color constants referenced throughout app"
  key_links:
    - from: "tailwind.config.js"
      to: "src/app/globals.css"
      via: "Tailwind directives + custom CSS variables"
      pattern: "@tailwind base"
    - from: "src/app/layout.tsx"
      to: "src/components/layout/Navbar.tsx"
      via: "import and render"
      pattern: "import.*Navbar"
---

<objective>
Bootstrap the Next.js application with Tailwind CSS, establish the design token system matching the existing prototype, and create the shared layout shell with Navbar and background components.

Purpose: Every subsequent plan builds on top of this scaffold. Getting the design tokens right here avoids rework later — the prototype's visual identity must transfer intact.
Output: A running Next.js 14 App Router project with Tailwind configured, prototype colors/fonts applied, shared layout, and reusable UI primitives (Panel, Btn, HexBg).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/STACK.md
@.planning/codebase/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js 14 project with Tailwind and dependencies</name>
  <files>package.json, next.config.js, tailwind.config.js, postcss.config.js, tsconfig.json, src/app/globals.css</files>
  <action>
    Initialize a new Next.js 14 project in the current directory using `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"`. Accept all defaults.

    After init, install additional dependencies:
    - `@supabase/supabase-js @supabase/ssr` — Supabase client and SSR helpers
    - `lucide-react` — Icon library (SVG icons, never emojis per SKILL.md)

    Then configure tailwind.config.js to extend the color palette with ALL prototype design tokens:
    ```
    colors: {
      bg: '#08080F',
      panel: '#111827',
      purple: { DEFAULT: '#9B72CF', light: '#B99FE0', dark: '#7B52AF' },
      gold: { DEFAULT: '#E8A838', light: '#F0C060' },
      teal: { DEFAULT: '#4ECDC4', light: '#7EDDD6' },
      muted: '#6B7280',
      border: '#1F2937',
    }
    ```

    In src/app/globals.css, add:
    - `@tailwind base; @tailwind components; @tailwind utilities;`
    - CSS variables for the color tokens as `--color-*` names
    - Base body styles: `background-color: #08080F; color: #F9FAFB; font-family: 'Barlow Condensed', sans-serif;`

    In next.config.js, ensure images and no strict mode issues. Keep minimal config.
  </action>
  <verify>
    <automated>cd C:/Users/gubje/Downloads/tft-clash && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Build completes with no errors. package.json lists next@14, @supabase/supabase-js, @supabase/ssr, lucide-react. tailwind.config.js has all 5 prototype color tokens. globals.css has Tailwind directives.</done>
</task>

<task type="auto">
  <name>Task 2: Create design constants, shared layout, and UI primitives</name>
  <files>src/lib/constants.ts, src/app/layout.tsx, src/app/page.tsx, src/components/ui/Panel.tsx, src/components/ui/Btn.tsx, src/components/ui/HexBg.tsx, src/components/layout/Navbar.tsx</files>
  <action>
    **src/lib/constants.ts** — Export the game constants extracted from prototype:
    - `PTS`: `{ 1: 8, 2: 7, 3: 6, 4: 5, 5: 4, 6: 3, 7: 2, 8: 1 }` (EMEA rulebook placement points)
    - `TIERS`: `['Player', 'Pro', 'Host']` with monthly prices
    - `RANKS`: Array of TFT rank names (Iron through Challenger)
    - `NAV_ITEMS`: `['home', 'standings', 'bracket', 'leaderboard', 'hof', 'archive', 'milestones', 'challenges', 'results', 'pricing']` (public items; scrims/admin added conditionally by auth role)
    - Color tokens re-exported for non-Tailwind usage

    **src/app/layout.tsx** — Root layout:
    - Import Playfair Display (weights 400, 700, 900) and Barlow Condensed (weights 400, 500, 600, 700, 800) from `next/font/google`
    - Apply font CSS variables to `<html>` element
    - Set metadata: title "TFT Clash", description "Competitive TFT tournament platform"
    - Wrap `{children}` with `<HexBg>` background and `<Navbar>` above content

    **src/components/ui/HexBg.tsx** — Background component:
    - Full-screen fixed div with bg color `#08080F`
    - Subtle radial gradient overlay in purple for depth (matches prototype aesthetic)
    - `pointer-events-none` so it doesn't block interactions

    **src/components/ui/Panel.tsx** — Card/panel primitive:
    - Accepts `children`, `className`, optional `title` string prop
    - Background `bg-panel` (`#111827`), rounded-xl, border `border-border`, padding p-6
    - If `title` provided, render it in Playfair Display, text-gold font-bold mb-4

    **src/components/ui/Btn.tsx** — Button primitive:
    - Accepts `children`, `onClick`, `variant` ('primary' | 'secondary' | 'ghost'), `disabled`, `className`
    - Primary: `bg-purple text-white hover:bg-purple-dark`
    - Secondary: `border border-purple text-purple hover:bg-purple/10`
    - Ghost: `text-muted hover:text-white`
    - All variants: `cursor-pointer transition-colors duration-200 rounded-lg px-4 py-2 font-medium` (min 44px touch target via py)
    - Disable button during async with `disabled:opacity-50 disabled:cursor-not-allowed`

    **src/components/layout/Navbar.tsx** — Navigation bar:
    - Client component (`'use client'`)
    - Render nav items from `NAV_ITEMS` constant using Next.js `<Link>` with `usePathname()` for active state
    - Active item highlighted with text-purple border-b-2 border-purple
    - Tier badge area (right side) — placeholder for auth state (will be wired in plan 03)
    - Branding: "TFT CLASH" in Playfair Display text-gold on left

    **src/app/page.tsx** — Placeholder home page:
    - Simple `<Panel>` with "TFT Clash" heading and "Season 16" subtitle
    - Sufficient to confirm layout renders correctly
  </action>
  <verify>
    <automated>cd C:/Users/gubje/Downloads/tft-clash && npm run dev &amp; sleep 8 &amp;&amp; curl -s http://localhost:3000 | grep -q "TFT" &amp;&amp; echo "OK" &amp;&amp; kill %1</automated>
  </verify>
  <done>Dev server serves a page at localhost:3000. Page renders Panel with "TFT Clash" heading. Navbar shows nav items. HexBg dark background visible. Build compiles without TypeScript errors.</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0 with no TypeScript errors
- `npm run dev` serves page at localhost:3000
- tailwind.config.js contains all 5 color tokens: bg, panel, purple, gold, teal
- src/lib/constants.ts exports PTS map with correct EMEA rulebook values {1:8, 2:7, 3:6, 4:5, 5:4, 6:3, 7:2, 8:1}
- Playfair Display and Barlow Condensed font variables present in layout.tsx
- Panel, Btn, HexBg, Navbar components exist and have no TypeScript errors
</verification>

<success_criteria>
1. Next.js 14 App Router project builds cleanly
2. Tailwind extended colors match prototype palette exactly
3. Shared layout shell (HexBg + Navbar + content) renders on every page
4. Core UI primitives (Panel, Btn) are ready for use by downstream plans
5. src/lib/constants.ts provides PTS, TIERS, RANKS, NAV_ITEMS for downstream use
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-auth/01-01-SUMMARY.md`
</output>
